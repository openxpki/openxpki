# trunk/package/debian/scep-client

TOPDIR=../../..
PERLNAME=OpenXPKI-Client-SCEPv2
SRCBASE=$(TOPDIR)/clients/perl/$(PERLNAME)
PACKAGE=libopenxpki-client-scep-perl
PKGDIR=../deb/client

# Makefile.inc contains common settings for all packages (checked in)
include ../Makefile.inc
# Makefile.local may be used locally to override settings (not checked in)
-include ../Makefile.local

