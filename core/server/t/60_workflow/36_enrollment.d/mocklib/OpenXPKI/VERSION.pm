# This mock module allows us to use the oxi libs directly from the
# git working repo rather than installing, which is what creates 
# the OpenXPKI::VERSION
#

package OpenXPKI::VERSION;
our $VERSION = '0.10.0';
1;

