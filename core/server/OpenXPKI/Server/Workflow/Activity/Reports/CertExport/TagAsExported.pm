package OpenXPKI::Server::Workflow::Activity::Reports::CertExport::TagAsExported;

# This class is no longer supported, it stays here to not break existing
# configs having the old unsupported export workflow installed
# which was the default in old default installs

use strict;
use base qw( OpenXPKI::Server::Workflow::Activity );

1;