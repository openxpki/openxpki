<button
    type="button"
    class="btn {{this.typeClass}} {{if @active "active"}}"
    {{on "click" this.handleClick}}
    ...attributes
>
    {{#if @icon}}<span class={{@icon}}></span>{{/if}}
    {{yield}}
</button>
