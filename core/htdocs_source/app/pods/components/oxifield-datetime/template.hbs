<BsDatetimepicker
    @date={{this.value}}
    @useCurrent={{if this.value false true}}
    @format={{this.format}}
    @change={{this.datePicked}}
    @openOnFocus={{true}}
    @placeholder={{@content.placeholder}}
/>
