<textarea
    class="form-control"
    rows={{this.rows}}
    cols={{this.cols}}
    value={{@content.value}}
    placeholder={{@content.placeholder}}
    {{on "input" this.onInput}}
    {{may-focus this true}}
/>
