/*
 * CUSTOM CONFIGURATIONS
 * Copy this file to "localconfig.js" and adjust the options to your needs.
 */

const OXI_LOCALCONFIG = {
    backendPath: 'cgi-bin/webui.fcgi',  // Protocol + host are prepended automatically
};
