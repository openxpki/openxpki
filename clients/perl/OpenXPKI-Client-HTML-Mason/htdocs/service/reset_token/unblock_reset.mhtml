%# unblock_reset.mhtml
%#
%# This is a widget that allows the user to reset (re-initialize) the current
%# workflow instance. It must be used within a form block to work correctly.

<p>To discard current activation codes and restart process:
<div class="form_button_bar">
	<span class="form_button_bar_button">
	<input type="submit" name="next_action" value="Restart"/>
</span>
</div>

