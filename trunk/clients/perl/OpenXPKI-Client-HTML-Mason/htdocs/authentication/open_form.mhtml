<%args>
    $session_id => undef
</%args>
  <div class="subheader"></div>
</div> <!-- header -->
<div id="main">
% my $title = $m->request_comp()->title();
%    $title =~ s/[^\/]+\//..\//g;
%    $title =~ s/\/[^\/]*$/\//;
%    $title =~ s/^\///;
        <form action="<% $title %>authentication/index.html" method="post">
          <input type="hidden" name="__session_id" value="<% $session_id %>"/>

