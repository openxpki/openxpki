<%args>
    $role => undef
</%args>
<& /service/open_form.mhtml &>
<h1><% i18nGettext ('I18N_OPENXPKI_CLIENT_HTML_MASON_CREATE_CSR_GET_ROLE_TITLE') %></h1>
<p>
  <% i18nGettext('I18N_OPENXPKI_CLIENT_HTML_MASON_CREATE_CSR_GET_ROLE_DESCRIPTION') %>
</p>
<select name="role">
%   foreach my $hrole (sort keys %roles)
%   {
  <option value="<% $hrole %>"><% $hrole %></option>
%   }
</select>
<& /service/send_form.mhtml &>
<& /service/close_form.mhtml &>
<dl>

<%init>
    ## read all roles from the server
    my $msg = $context->{client}->send_receive_command_msg("get_cert_profiles");
    my %roles = %{$msg->{PARAMS}};

    ## return the configuration number if it exists
    if (defined $role and
        exists $roles{$role})
    {
        ## this is the ID of the profile
        return $role;
    }

    ## if we have only one configuration
    ## then we have not to ask the user
    if (scalar keys %roles == 1)
    {
        my @array = keys %roles;
        $role = pop @array;
        return $role;
    }

    ## ok, we have to ask the user
</%init>
